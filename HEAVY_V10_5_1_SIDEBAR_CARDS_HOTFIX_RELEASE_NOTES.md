# Heavy v10.5.1 — Sidebar Background + Small Cards Scroll Hotfix

Scope is intentionally narrow:

- Extend the navy sidebar background to the full page height using a background-only layer.
- Do not stretch sidebar content or change route/template logic.
- Remove scrollbars from small KPI/stat cards.
- Clamp long small-card text instead of adding internal scroll.
- Bump CSS cache version to `10.5.1`.

No backend code, route logic, or main.py changes were made.
