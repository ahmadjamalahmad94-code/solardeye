# تشغيل الموقع محليًا قبل الرفع على الاستضافة

## أسرع طريقة (نقرة واحدة)

١. روح على مجلد `solardeya` على الديسكتوب.
٢. اعمل **دبل-كليك** على ملف **`run_local.bat`**.
٣. أول مرة بس راح ياخد دقيقة-اثنين عشان ينزّل المكتبات.
٤. بعدها راح يفتح المتصفح تلقائيًا على:
   `http://localhost:5000`

> لو ما اشتغل الـ `.bat`، استخدم البديل: كليك يمين على
> `start_local_server.ps1` → **Run with PowerShell**.

## الفرق بين النسخة المحلية والإنتاج

| العنصر | محلي (.env.local) | إنتاج (.env) |
|---|---|---|
| قاعدة البيانات | SQLite (ملف `solar_local.db`) | Postgres على Render |
| `SESSION_COOKIE_SECURE` | false (يشتغل على http) | true |
| `APP_ENV` | development | production |
| المزامنة التلقائية | معطّلة | مفعّلة |

> كل التغييرات اللي بتعملها محليًا بتنحفظ في ملف `solar_local.db`،
> وما بتأثر إطلاقًا على بيانات الإنتاج على Render.

## معلومات تسجيل الدخول للنسخة المحلية

- المستخدم: `admin`
- كلمة المرور: `Admin@Local2026`

(محددة في `.env.local`. غيّرها لو حابب.)

## الإيقاف

من نافذة الـ CMD/PowerShell اضغط `Ctrl + C`.

## استكشاف الأخطاء

- **"Python not found"** → نزّل Python 3.11 من [python.org](https://www.python.org/downloads/) وعلّم خيار **Add to PATH** أثناء التثبيت.
- **خطأ في تثبيت المكتبات** → افتح PowerShell كمسؤول وشغّل:
  `python -m pip install --upgrade pip setuptools wheel`
- **البورت 5000 مشغول** → غيّر `PORT=5000` في `.env.local` لرقم تاني (مثلاً 5050).

## قبل الرفع للاستضافة

تأكد إنه:
- ملف `.env` (الإنتاجي) ما تغيّر.
- ملف `.env.local` و `solar_local.db` مستثنيين من Git (موجودين بالـ `.gitignore`).
- التغييرات اللي حابب ترفعها متّ commit.
